# CMake package configuration file for the SofaQt3D "plugin"

@PACKAGE_INIT@

find_package(SofaBase REQUIRED)

if(NOT TARGET SofaQt3D)
    include("${CMAKE_CURRENT_LIST_DIR}/SofaQt3DTargets.cmake")
endif()

check_required_components(SofaQt3D)
